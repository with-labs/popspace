// slightly enlarge the button
// when a user hovers over it
export const trayItemHover = {
  scale: 1.1,
};

// shrink the button slightly when a user clicks
// the button
export const trayItemClick = {
  scale: 0.9,
};
