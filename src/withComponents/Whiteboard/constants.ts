import palette from '../../theme/palette';

// we don't actually use this as a 'color'
export const ERASER_COLOR = palette.grey[500];
export const STROKE_WIDTH = 2;
export const ERASER_WIDTH = 8;
export const COLORS = [
  ERASER_COLOR,
  palette.ink.main,
  palette.mandarin.dark,
  palette.cherry.dark,
  palette.turquoise.dark,
  palette.lavender.dark,
];
